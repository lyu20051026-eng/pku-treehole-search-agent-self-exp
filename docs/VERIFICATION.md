# 本地验证记录（2026-09-11）

## 自动验证

使用本地 Python 3.14.3 虚拟环境：

```bash
venv/bin/python -m unittest discover -s tests -v
venv/bin/python -m compileall -q agent.py client.py hot_topics tests
git diff --check
```

47 项测试通过，覆盖分页去重和乱序、时间窗口、真实计数字段、评论父子关系、关键词 DF、互动独立召回、上下文分块、引用校验、有限纠正重试、模型中断与截断、离线重放和旧模式 1～3 的模拟回归。未在 Python 3.8 运行时实测；已检查 3.8 语法兼容。

## 真实接口与模型联调

真实采集窗口为北京时间 2026-09-09 17:42:57 至 2026-09-10 17:42:57。随后在同一导出上重放模型，避免重复采集导致验收素材变化。

- 扫描 167 页、5000 条记录，去重后 4972 帖，其中窗口内 4455 帖；最终入选 30 帖。
- 扫描达到默认上限，且观察到时间乱序，因此报告明确标为部分覆盖，不能视为全站完整统计。
- 取得 265 条评论；30 帖实际评论数均与当时接口的 reply 一致。2 处父评论未取得，显式标记缺失。
- 核对全部入选帖时间、收藏 likenum、点赞 praise_num、评论 reply；核对已解析父评论 ID 与文本。
- DeepSeek 总结成功，命令退出码 0：5/5 素材块、10 个话题、54 个不同有效出处；共 7 次模型调用，含 1 次纠正。人工对照各话题代表来源检查概述和限定。
- 真实联调发现并修复：data.total/comment_total 是动态分页提示；模型思考及过长输出可能耗尽输出预算。修复均有离线回归用例。

真实帖子、导出、日志、会话和私有配置均留在被 Git 忽略的本地目录，不随代码提交。真实联调不保证后续模型每次产生相同话题或文字，也不等价于对所有总结句子的自动事实核验。

## 用户复验

在仓库中激活 venv 后运行 `python agent.py`，选择 `4`，选择默认 24 小时或 72/168 小时。每次生成独立目录，检查 summary.md、sources.md 和 posts.json。完整的评分说明、人工抽查清单，以及仅采集/离线重放调试命令见 [HOT_TOPICS.md](HOT_TOPICS.md)。旧模式已通过模拟回归，仍建议使用自己的常用查询做交互复验。
